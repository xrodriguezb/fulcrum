{
  "name": "fulcrum-repo-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Repository level tooling only. The application frontend lives in web/.",
  "scripts": {
    "commitlint": "commitlint"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2"
  }
}
